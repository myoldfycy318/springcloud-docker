# 服务器访问端口
server:
  port: 8010

# 数据库基本配置
spring:
  application:
    name: microservice-provider-user
  datasource:
   url: jdbc:mysql://192.168.20.3:3306/test
   username: root
   password: root
   driver-class-name: com.mysql.jdbc.Driver
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: update
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    instance:
      prefer-ip-address: true
            